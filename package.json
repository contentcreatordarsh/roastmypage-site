{
  "name": "roast-my-landing-page-v2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.12"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
